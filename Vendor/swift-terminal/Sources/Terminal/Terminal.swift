/// A namespace for terminal-related primitives.
///
/// `Terminal` is intentionally uninhabited. Use its static members and nested
/// types to query terminal state and build terminal control sequences.
@frozen
public enum Terminal {
}
