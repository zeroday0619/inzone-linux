extension Terminal {

    /// A namespace for Select Graphic Rendition (SGR) escape sequences.
    ///
    /// SGR sequences are ANSI Control Sequence Introducer commands ending in
    /// `m`. They control text attributes such as color, bold, underline, and
    /// other display styles.
    @frozen
    public enum SGR {
    }
}
